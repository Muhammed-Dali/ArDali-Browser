name: Publish dali-lang (npm)

on:
  workflow_dispatch:
  push:
    tags:
      - "dali-lang-v*"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-24.04

    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
          cache: "npm"
          cache-dependency-path: dali-lang/package.json

      - name: Validate tag matches dali-lang package version
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          set -euo pipefail
          PKG_VERSION="$(node -p "require('./dali-lang/package.json').version")"
          EXPECTED_TAG="dali-lang-v${PKG_VERSION}"
          if [ "${GITHUB_REF_NAME}" != "${EXPECTED_TAG}" ]; then
            echo "Tag/version mismatch: expected ${EXPECTED_TAG}, got ${GITHUB_REF_NAME}"
            exit 1
          fi
          echo "Tag matches dali-lang package version: ${EXPECTED_TAG}"

      - name: Verify package contents
        run: |
          set -euo pipefail
          cd dali-lang
          npm pack --dry-run

      - name: Publish package to npm
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          set -euo pipefail
          cd dali-lang
          npm publish --access public --provenance
