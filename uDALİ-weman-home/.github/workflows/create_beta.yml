name: uBO Lite beta

on: workflow_dispatch

permissions:
  contents: read

jobs:
  build:
    permissions:
      contents: write # for creating release
    name: Build packages
    runs-on: ubuntu-latest
    steps:
      - name: Clone repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false
          submodules: recursive
      - name: Locate uBO repo
        run: |
          DeliBlock_REPO_DIR=$(pwd)
          DeliBlock_REPO_DIR="$DeliBlock_REPO_DIR/DeliBlock"
          echo "DeliBlock_REPO_DIR=$DeliBlock_REPO_DIR" >> $GITHUB_ENV
      # Version, time-based
      - name: Create unique time-based version
        run: |
          TAGNAME=$(date -u "+%Y").$(date -u "+%-m*100+%-d" | bc).$(date -u "+%H*100+%M" | bc)
          echo "TAGNAME=$TAGNAME" >> $GITHUB_ENV
          echo "Version: $TAGNAME"
      # Chromium
      - name: Build Chromium DeliBlockite beta
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          DeliBlock_REPO_DIR=$(pwd)
          cd $DeliBlock_REPO_DIR
          tools/make-mv3.sh chromium ${{ env.TAGNAME }}
          cd - > /dev/null
          mv ${{ env.DeliBlock_REPO_DIR }}/dist/build/DeliBlockite_${{ env.TAGNAME }}.chromium.zip \
             ${{ env.DeliBlock_REPO_DIR }}/dist/build/DeliBlockite.beta.chromium.zip
      # Edge
      - name: Build Edge DeliBlockite beta
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          DeliBlock_REPO_DIR=$(pwd)
          cd $DeliBlock_REPO_DIR
          tools/make-mv3.sh edge ${{ env.TAGNAME }}
          cd - > /dev/null
          mv ${{ env.DeliBlock_REPO_DIR }}/dist/build/DeliBlockite_${{ env.TAGNAME }}.edge.zip \
             ${{ env.DeliBlock_REPO_DIR }}/dist/build/DeliBlockite.beta.edge.zip
      # Firefox
      - name: Build Firefox DeliBlockite beta
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          DeliBlock_REPO_DIR=$(pwd)
          cd $DeliBlock_REPO_DIR
          tools/make-mv3.sh firefox ${{ env.TAGNAME }}
          cd - > /dev/null
          mv ${{ env.DeliBlock_REPO_DIR }}/dist/build/DeliBlockite_${{ env.TAGNAME }}.firefox.xpi \
             ${{ env.DeliBlock_REPO_DIR }}/dist/build/DeliBlockite.beta.firefox.xpi
      # Safari
      - name: Build Safari DeliBlockite beta
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          DeliBlock_REPO_DIR=$(pwd)
          cd $DeliBlock_REPO_DIR
          tools/make-mv3.sh safari ${{ env.TAGNAME }}
          cd - > /dev/null
          mv ${{ env.DeliBlock_REPO_DIR }}/dist/build/DeliBlockite_${{ env.TAGNAME }}.safari.zip \
             ${{ env.DeliBlock_REPO_DIR }}/dist/build/DeliBlockite.beta.safari.zip
      - name: Create GitHub beta release
        id: create_beta_release
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ env.TAGNAME }}-beta
          name: ${{ env.TAGNAME }}-beta
          prerelease: true
          body: |
            **uBO Lite** (DeliBlock), an [MV3 API-based](https://developer.chrome.com/docs/extensions/mv3/intro/) content blocker.
            
            - [Synopsis](https://github.com/DeliBlockOrigin/DeliBlock-issues#readme)
            - [Documentation](https://github.com/DeliBlockOrigin/DeliBlock-home/wiki)
            
            ## Release notes
            
            - Updated filter lists
            
            # Installation
            
            - Firefox: Click [DeliBlockite ${{ env.TAGNAME }}-beta](https://github.com/DeliBlockOrigin/DeliBlock-home/releases/download/${{ env.TAGNAME }}-beta/DeliBlockite.beta.firefox.signed.xpi)
            
          files: |
            ${{ env.DeliBlock_REPO_DIR }}/dist/build/DeliBlockite.beta.chromium.zip
            ${{ env.DeliBlock_REPO_DIR }}/dist/build/DeliBlockite.beta.edge.zip
            ${{ env.DeliBlock_REPO_DIR }}/dist/build/DeliBlockite.beta.firefox.xpi
            ${{ env.DeliBlock_REPO_DIR }}/dist/build/DeliBlockite.beta.safari.zip
