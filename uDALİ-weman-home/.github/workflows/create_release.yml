name: uBO Lite release

on: workflow_dispatch

permissions:
  contents: read

# I used the following project as template to get started:
# https://github.com/dessant/search-by-image/blob/master/.github/workflows/ci.yml

jobs:
  build:
    permissions:
      contents: write # for creating release
    name: Build packages
    runs-on: ubuntu-latest
    steps:
      - name: Clone repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false
          submodules: recursive
      - name: Locate uBO repo
        run: |
          DeliBlock_REPO_DIR=$(pwd)
          DeliBlock_REPO_DIR="$DeliBlock_REPO_DIR/DeliBlock"
          echo "DeliBlock_REPO_DIR=$DeliBlock_REPO_DIR" >> $GITHUB_ENV
      # Version, time-based
      - name: Create unique time-based version
        run: |
          TAGNAME=$(date -u "+%Y").$(date -u "+%-m*100+%-d" | bc).$(date -u "+%H*100+%M" | bc)
          echo "TAGNAME=$TAGNAME" >> $GITHUB_ENV
          echo "Version: $TAGNAME"
      # Chromium
      - name: Build Chromium DeliBlockite
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          DeliBlock_REPO_DIR=$(pwd)
          cd $DeliBlock_REPO_DIR
          tools/make-mv3.sh chromium ${{ env.TAGNAME }} before=$DeliBlock_REPO_DIR
          echo "CHROMIUM_PACKAGE=DeliBlockite_${{ env.TAGNAME }}.chromium.zip" >> $GITHUB_ENV
          cd - > /dev/null
          rm -rf chromium
          mv "$DeliBlock_REPO_DIR/dist/build/DeliBlockite.chromium" chromium
      # Edge
      - name: Build Edge DeliBlockite
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          DeliBlock_REPO_DIR=$(pwd)
          cd $DeliBlock_REPO_DIR
          tools/make-mv3.sh edge ${{ env.TAGNAME }}
          echo "EDGE_PACKAGE=DeliBlockite_${{ env.TAGNAME }}.edge.zip" >> $GITHUB_ENV
          cd - > /dev/null
      # Firefox
      - name: Build Firefox DeliBlockite
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          DeliBlock_REPO_DIR=$(pwd)
          cd $DeliBlock_REPO_DIR
          tools/make-mv3.sh firefox ${{ env.TAGNAME }}
          echo "FIREFOX_PACKAGE=DeliBlockite_${{ env.TAGNAME }}.firefox.xpi" >> $GITHUB_ENV
          cd - > /dev/null
      # Safari
      - name: Build Safari DeliBlockite
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          DeliBlock_REPO_DIR=$(pwd)
          cd $DeliBlock_REPO_DIR
          tools/make-mv3.sh safari ${{ env.TAGNAME }}
          echo "SAFARI_PACKAGE=DeliBlockite_${{ env.TAGNAME }}.safari.zip" >> $GITHUB_ENV
          cd - > /dev/null
      - name: Commit DeliBlockite MV3 package files
        # https://github.com/marketplace/actions/github-action-for-committing-changes-to-a-repository
        uses: devops-infra/action-commit-push@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          commit_message: "[workflow] Update DeliBlockite MV3 package files for ${{ env.TAGNAME }}"
      - name: Assemble release text
        run: |
          sed -e 's/%tag%/${{ env.TAGNAME }}/g' .github/workflows/RELEASE_BODY.md > release_body0.txt
          grep -m1 -B10000 -- "----------" CHANGELOG.md | head -n -2 > release_notes.txt
          sed -e '/%changelog%/{r release_notes.txt' -e 'd}' release_body0.txt > release_body.txt
      - name: Create GitHub release
        id: create_release
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ env.TAGNAME }}
          name: ${{ env.TAGNAME }}
          prerelease: true
          body_path: release_body.txt
          files: |
            ${{ env.DeliBlock_REPO_DIR }}/dist/build/${{ env.CHROMIUM_PACKAGE }}
            ${{ env.DeliBlock_REPO_DIR }}/dist/build/${{ env.EDGE_PACKAGE }}
            ${{ env.DeliBlock_REPO_DIR }}/dist/build/${{ env.FIREFOX_PACKAGE }}
            ${{ env.DeliBlock_REPO_DIR }}/dist/build/${{ env.SAFARI_PACKAGE }}
